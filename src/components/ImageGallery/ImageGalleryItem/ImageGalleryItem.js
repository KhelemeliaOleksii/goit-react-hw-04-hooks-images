const ImageGalleryItem = (props) => {
    return (
        <div>
            <p>Here we are, we are items!</p>
            <p>Here we are, we are items!</p>
        </div>
    )
};

export default ImageGalleryItem;